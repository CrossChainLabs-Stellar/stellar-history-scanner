import { Node } from '../node';
/**
 * A quorum contains a quorum slice for every node in the quorum
 */
export default function isQuorum(potentialQuorum: Node[]): boolean;
//# sourceMappingURL=isQuorum.d.ts.map